#ifndef __SCENE_INTRO_H__
#define __SCENE_INTRO_H__

#include <kos.h>
#include "scene.h"

void drawSceneIntro();
void loadIntro(scene *self);
void loadIntro2(scene *self);
void updateIntro(cont_state_t *state, scene *self);
void newIntro(cont_state_t *state, scene *self);
void newIntro2(cont_state_t *state, scene *self);



#endif
