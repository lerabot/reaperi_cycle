#ifndef __SCENE_MENU_H__
#define __SCENE_MENU_H__

#include <kos.h>
#include "scene.h"

void loadMenu(scene *self);
void freeMenu(scene *self);
void renderMenuScreen(scene *self);
void updateMenu(scene *self);

#endif
